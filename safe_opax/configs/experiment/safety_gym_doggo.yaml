# @package _global_
defaults:
  - override /environment: safety_gym

training:
  epochs: 200
  safe: true
  action_repeat: 2
  episodes_per_epoch: 5

environment:
  safety_gym:
    robot_name: doggo
    level: 1

agent:
  exploration_steps: 0
  actor:
    initialization_scale: 1.