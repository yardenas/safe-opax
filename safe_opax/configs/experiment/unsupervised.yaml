# @package _global_
defaults:
  - override /environment: safe_adaptation_gym

environment:
  safe_adaptation_gym:
    task: unsupervised

training:
  epochs: 100
  safe: true
  action_repeat: 2
  exploration_steps: 250000

agent:
  exploration_strategy: opax
  exploration_steps: 250000
